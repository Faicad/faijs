# Git hooks (lefthook). Keep these local checkpoints fast; CI owns the full
# repository-wide gate matrix.
# Install: `node scripts/install-lefthook.mjs` (runs automatically via postinstall).

pre-commit:
  jobs:
    - name: normalize line endings to LF (staged)
      glob: '**/*'
      run: git add --renormalize {staged_files}

    - name: translation pairing (staged records)
      glob: '*.i18n.yaml'
      exclude:
        - '.agents/notes/archived/**'
      run: node_modules/.bin/tsx scripts/verify-translation-pairing.ts --cached {staged_files}

    - name: archived agent notes
      glob: '.agents/notes/archived/**'
      run: node_modules/.bin/tsx scripts/verify-archived-agent-notes.ts

    - name: export jsdoc (repo-wide API completeness)
      run: node_modules/.bin/tsx scripts/verify-export-jsdoc.ts

    - name: eslint (staged)
      glob: '{src,packages/core/src,packages/stdlib/src,packages/mech-lib/src}/**/*.{ts,tsx}'
      run: node_modules/.bin/eslint --fix {staged_files}
      stage_fixed: true

    - name: whitespace (staged)
      run: git diff --cached --check

pre-merge-commit:
  jobs:
    - name: translation pairing (staged records)
      glob: '*.i18n.yaml'
      exclude:
        - '.agents/notes/archived/**'
      run: node_modules/.bin/tsx scripts/verify-translation-pairing.ts --cached {staged_files}

    - name: archived agent notes
      glob: '.agents/notes/archived/**'
      run: node_modules/.bin/tsx scripts/verify-archived-agent-notes.ts

    - name: export jsdoc (repo-wide API completeness)
      run: node_modules/.bin/tsx scripts/verify-export-jsdoc.ts

pre-push:
  jobs:
    - name: typecheck
      run: npm run typecheck