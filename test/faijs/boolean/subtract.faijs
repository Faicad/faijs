// apiVersion: 1
// Tests subtract operation
export default async (cad) => {
  const part0_v0 = cad.box({ size: 30 })
  const part0_v1 = cad.sphere({ radius: 10, center: [0, 0, 0] })
  const part0_v2 = cad.subtract(part0_v0, part0_v1)
  return { shape: part0_v2, name: 'subtract-test' }
}
