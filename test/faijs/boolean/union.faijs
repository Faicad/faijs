// apiVersion: 1
// Tests union operation
export default async (cad) => {
  const part0_v0 = cad.box({ size: 20 })
  const part0_v1 = cad.box({ size: 20, center: [10, 0, 0] })
  const part0_v2 = cad.union(part0_v0, part0_v1)
  return { shape: part0_v2, name: 'union-test' }
}
