// apiVersion: 1
// Tests extrude operation
export default async (cad) => {
  const part0_v0 = cad.box({ size: [50, 50, 10] })
  const part0_v1 = cad.extrude({ length: 5 }, part0_v0)
  return { shape: part0_v1, name: 'extrude-test' }
}
