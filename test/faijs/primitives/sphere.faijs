// apiVersion: 1
// Tests sphere with different parameters
export default async (cad) => {
  const part0_v0 = cad.sphere({ radius: 10 })
  const part0_v1 = cad.sphere({ radius: 8, center: [0, 0, 5] })
  return { shape: part0_v1, name: 'sphere-variants' }
}
