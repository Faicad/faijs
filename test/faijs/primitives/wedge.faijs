// apiVersion: 1
// Tests wedge primitive
export default async (cad) => {
  const part0_v0 = cad.wedge({ width: 20, height: 15, angle: 45, length: 30 })
  return { shape: part0_v0, name: 'wedge-test' }
}
